plugins {
    id 'java'
    id 'jacoco'
    id 'maven-publish'
}

group = 'dev.linkedlogics'
version = '1.0.0-SNAPSHOT'
sourceCompatibility = '11'

configurations {
	compileOnly {
		extendsFrom annotationProcessor
	}
}

repositories {
	mavenLocal()
	mavenCentral()
}

test {
    useJUnitPlatform()
    finalizedBy jacocoTestReport
}

jacocoTestReport {
    dependsOn test // tests are required to run before generating the report
}

jacoco {
    reportsDirectory = file("$buildDir/reports/jacoco")
}

dependencies {
	implementation 'dev.linkedlogics:linkedlogics:1.0.0-SNAPSHOT'
	implementation 'com.fasterxml.jackson.core:jackson-databind:2.13.3'
	implementation 'org.springframework:spring-jdbc:5.3.22'
	implementation 'org.jooq:jooq:3.17.2'
	implementation 'com.zaxxer:HikariCP:4.0.3'
	compileOnly 'org.projectlombok:lombok:1.18.22'
	annotationProcessor 'org.projectlombok:lombok:1.18.22'
	testImplementation 'org.junit.jupiter:junit-jupiter-api:5.9.0'
	testImplementation 'org.junit.jupiter:junit-jupiter-params:5.9.0'
	testImplementation 'org.assertj:assertj-core:3.22.0'
	testImplementation 'org.mockito:mockito-core:4.8.0'
	testImplementation 'org.mockito:mockito-junit-jupiter:4.8.0'
	implementation 'com.h2database:h2:2.1.214'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.9.0'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'dev.linkedlogics'
            artifactId = 'linkedlogics-jdbc'
            version = '1.0.0-SNAPSHOT'

            from components.java
        
            pom {
                name = 'LinkedLogics JDBC'
                description = 'Linked-Logics Framework'
                url = 'http://www.linkedlogics.dev'
                licenses {
                    license {
                        name = 'The Apache License, Version 2.0'
                        url = 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                    }
                }
                developers {
                    developer {
                        id = 'rdavudov'
                        name = 'Rajab Davudov'
                        email = 'radjab@gmail.com'
                    }
                }
                scm {
                    connection = 'scm:git:git://linkedlogics/linkedlogics-jdbc.git'
                    developerConnection = 'scm:git:ssh://linkedlogics/linkedlogics-jdbc.git'
                    url = 'http://www.linkedlogics.dev'
                }
            }
        }
    }
}


